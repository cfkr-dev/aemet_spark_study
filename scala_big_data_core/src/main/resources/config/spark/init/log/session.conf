start-spark-session = "Starting Spark session"
end-spark-session-check-stats = "Session completed. Check the stats here: %s"
end-spark-session-press-any-key = "Press any key to close the spark session"
end-spark-session = "Spark session closed successfully."
start-queries = "Starting queries"
end-queries = "Completed queries"
